{
    "name": "@agentping/webhook",
    "version": "0.1.0",
    "description": "Webhook notification adapter for AgentPing",
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "test": "vitest",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@agentping/core": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "vitest": "^1.0.0"
    }
}
