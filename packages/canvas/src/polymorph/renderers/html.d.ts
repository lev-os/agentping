/**
 * HTML Renderer
 *
 * Renders polymorph primitives to standalone HTML strings.
 */
import type { PolymorphPrimitive, RenderOptions } from '../types.js';
export declare function renderToHTML(primitives: PolymorphPrimitive[], options: RenderOptions): string;
