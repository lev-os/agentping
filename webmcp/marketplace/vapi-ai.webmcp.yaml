# Generated by heuristic-webmcp.ts (no LLM — 80% skeleton)
# Review and refine: result selectors, descriptions, parameter validation
name: vapi
version: "0.1.0"
match:
  - "https://vapi.ai/*"
  - "https://www.vapi.ai/*"

tools:
  - name: talk_to_vapigive_it_a_try
    description: "Click "Talk to vapigive it a Try" on vapi"
    parameters: {}
    actions:
      - type: click
        selector: "[aria-label="Talk to vapi"]"
    result:
      type: wait
      timeout: 3000

  - name: go_to_read_the_docs
    description: "Navigate to read the docs page"
    parameters: {}
    actions:
      - type: click
        selector: "[aria-label="read the docs"]"
    result:
      type: wait
      timeout: 5000

  - name: go_to_configuration_points4_2k_docs
    description: "Navigate to CONFIGURATION POINTS4.2K+Docs page"
    parameters: {}
    actions:
      - type: click
        selector: "[aria-label="Docs-external-redirect"]"
    result:
      type: wait
      timeout: 5000

  - name: get_page_title
    description: "Get the current page title"
    parameters: {}
    actions:
      - type: evaluate
        script: "document.title"
    result:
      type: immediate
