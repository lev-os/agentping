# Generated by heuristic-webmcp.ts (no LLM — 80% skeleton)
# Review and refine: result selectors, descriptions, parameter validation
name: mem0
version: "0.1.0"
match:
  - "https://mem0.ai/*"
  - "https://www.mem0.ai/*"

tools:
  - name: go_to_pricing
    description: "Navigate to Pricing page"
    parameters: {}
    actions:
      - type: click
        selector: "a[name="Fit"]"
    result:
      type: wait
      timeout: 5000

  - name: go_to_home_secondary_pricinghomepric
    description: "Navigate to home_secondary_pricingHomePricing page"
    parameters: {}
    actions:
      - type: click
        selector: "a[name="Dark/Large"]"
    result:
      type: wait
      timeout: 5000

  - name: get_page_title
    description: "Get the current page title"
    parameters: {}
    actions:
      - type: evaluate
        script: "document.title"
    result:
      type: immediate
